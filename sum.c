#include <stdio.h>
int main()
{
    float a,b;
    printf("Enter the numbers: ");
    scanf("%f%f",&a,&b);
    if(a>0 && b>0)
    if(a+b>40)
    printf("sum=%f",(a+b));
    else
    // printf("Not same");
    return 0;
}